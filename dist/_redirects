# Netlify redirects file for SPA routing
# This file provides fallback routing for platforms that support it
# For GitHub Pages, we rely on 404.html instead

# Redirect all unmatched routes to index.html for SPA handling
/*    /index.html   200

# Redirect old specialty URLs to new format (if needed)
/specialties/*  /specialty/:splat  301

# Ensure assets are served correctly
/assets/*  /assets/:splat  200
/content/*  /content/:splat  200